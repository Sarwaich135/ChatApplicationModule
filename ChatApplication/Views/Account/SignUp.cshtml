@model ChatApplication.DTOs.UserDTO

<h3 id="clientName"
    style="text-align:center;
        font-weight:bold">
    Let's SignUp
</h3>

@* <div style="text-align:center;>
    @using (Html.BeginForm("SignUp", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
    {
    @Html.AntiForgeryToken()
    <div class="form-group">
        <label>Email</label>
        @Html.TextBoxFor(model => model.email, new { @class = "form-control", type = "email", @style = "width: 20%" })
        @Html.ValidationMessageFor(model => model.email)
    </div>
    <div class="form-group">
        <label>Username</label>
        @Html.TextBoxFor(model => model.username, new { @class = "form-control", type = "username", @style = "width: 20%" })
        @Html.ValidationMessageFor(model => model.username)
    </div>
    <div class="form-group">
        <label>Password</label>
        @Html.PasswordFor(model => model.password, new { @class = "form-control", @style = "width: 20%" })
        @Html.ValidationMessageFor(model => model.password)
    </div>
    <br />
    <button type="submit" class="btn btn-primary">Signup</button>
    }

</div> *@


<!-- Sign Up -->
<div class="techwave_fn_sign">
    @using (Html.BeginForm("SignUp", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
    {
        @Html.AntiForgeryToken()
        <div class="sign__content">
            <h1 class="logo">Designed by Frenify</h1>
            <div class="signup">
                <div class="form__content">
                    <div class="form__title">Sign Up</div>
                    <div class="form__username">
                        <label for="username">Username</label>
                        @Html.TextBoxFor(model => model.username, new { @class = "form-control", type = "username"})
                        @Html.ValidationMessageFor(model => model.username)
                    </div>
                    <div class="form__email">
                        <label for="email">Email</label>
                        @Html.TextBoxFor(model => model.email, new { @class = "form-control", type = "email" })
                        @Html.ValidationMessageFor(model => model.email)
                    </div>
                    <div class="form__pass">
                        <label for="user_password">Password</label>
                        @Html.PasswordFor(model => model.password, new { @class = "form-control" })
                        @Html.ValidationMessageFor(model => model.password)
                    </div>
                    <div class="form__submit">
                        <label class="fn__submit">
                            <input type="submit" name="submit" value="Signup">
                        </label>
                    </div>
                </div>
            </div>
           @*  <div class="sign__desc">
                <p>Don't have an account? <a href="sign-in.html">Sign In</a></p>
            </div> *@
        </div>
    }
</div>
<!-- !Sign Up -->